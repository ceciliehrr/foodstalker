---
import { Image } from "astro:assets";
import DifficultyBadge from "../DifficultyBadge.vue";
import { getCategoryEmoji } from "../../utils/categoryEmojis";

export interface Props {
  title: string;
  emoji: string;
  href: string;
  image: string;
  description: string;
  date: string;
  recipe: any;
}
const { href, title, emoji, image, description, date, recipe } = Astro.props;
var months = [
  "Januar",
  "Februar",
  "Mars",
  "April",
  "Mai",
  "Juni",
  "Juli",
  "August",
  "September",
  "Oktober",
  "November",
  "Desember",
];
function convertDate(date_str) {
  const temp_date = date_str.split("-");
  return (
    temp_date[2] + ". " + months[Number(temp_date[1]) - 1] + " " + temp_date[0]
  );
}
const convertedDate = convertDate(date);
---

<a id={title} href={href} class="fs-card">
  <div class="fs-card__image">
    <Image
      class="fs-card__picture"
      src={image}
      alt={title}
      width={960}
      height={960}
      fit="contain"
      position="center"
      quality={80}
      format="webp"
      decoding="async"
      loading="lazy"
      sizes="(min-width: 64rem) 12rem, (max-width: 768px) 100vw, 90vw"
    />
  </div>
  <div class="fs-card__content">
    <div class="fs-card__text">
      <h2>
        {title}
      </h2>
      <p>
        {description}
      </p>
    </div>
    <div class="fs-card__footer">
      <DifficultyBadge recipe={recipe} client:idle />

      <div class="fs-card__footer-circle">
        <span>{getCategoryEmoji(emoji)}</span>
      </div>
    </div>
  </div>
</a>

<style lang="scss">
  @use "sass:map";
  @use "../../styles/variables/" as *;
  @use "../../styles/mixins/breakpoints" as *;
  @use "../../styles/mixins/" as *;
  .fs-card {
    display: flex;
    flex-direction: column;
    border: 1px solid var(--fs-gray-400);
    border-left-width: 1px;
    border-bottom-width: 1px;
    border-right-width: 1px;
    border-radius: var(--radius-sm);
    background-color: white;
    color: var(--fs-black);
    text-decoration: none;
    box-shadow: 0px 10px 15px -3px rgba(64, 89, 90, 0.1);

    @include bp("tablet-big-up") {
      flex-direction: row;
      border-top-width: 1px;
      max-width: 36rem;
    }
    &:hover {
      background-color: var(--fs-gray-100);
      color: var(--fs-black);
      outline: 2px solid var(--fs-berries-500);
    }
    &:focus-visible {
      outline: 2px solid var(--fs-berries-500);
      background-color: var(--fs-gray-100);
    }
    &:active {
      background-color: var(--fs-gray-200);
      color: var(--fs-black);
    }
    &__image {
      flex-shrink: 0;
      background: var(--fs-gray-100);
      line-height: 0;

      @include bp("tablet-big-up") {
        position: relative;
        flex: 0 0 12rem;
        width: 12rem;
        align-self: stretch;
        min-height: 0;
        overflow: hidden;
        background: transparent;
        border-top-left-radius: 0.5rem;
        border-bottom-left-radius: 0.5rem;
      }
    }
    &__picture {
      border-top-left-radius: 0.5rem;
      border-top-right-radius: 0.5rem;
      display: block;
      width: 100%;
      max-width: 100%;
      height: auto;
      object-fit: contain;
      object-position: center;

      @include bp("tablet-big-up") {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        max-width: none;
        border-radius: 0;
        border-top-left-radius: 0.5rem;
        border-bottom-left-radius: 0.5rem;
        object-fit: cover;
        object-position: center;
      }
    }
    &__content {
      display: flex;
      flex-direction: column;
      flex: 1 1 0%;
      padding: 1rem;
    }

    &__footer {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      margin-top: 0.75rem;
      justify-content: flex-end;

      &-time {
        display: flex;
        align-items: center;
        gap: 0.25rem;
        color: var(--fs-gray-500);
        font-size: 0.875rem;
        font-weight: 500;
      }

      &-circle {
        display: grid;
        place-items: center;
        background: var(--fs-gray-100);
        border: 1px solid var(--fs-gray-300);
        border-radius: var(--radius-full);
        width: 2.5rem;
        height: 2.5rem;
        font-size: 1rem;
        box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
      }
    }
    &__text {
      display: flex;
      flex-direction: column;
      flex: 1 1 0%;

      & h2 {
        @include get-text("fs-h6");
        margin-bottom: 1rem;
      }
      & p {
        @include get-text("fs-body1");
        margin-bottom: 0.75rem;
      }
    }
  }
</style>
